# describes the mcp servers to use
mcpServers:
  filesystem:
    type: stdio
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-filesystem"
      - .

# describes the llm configuration
llm:
  # OpenRouter configuration for testing

  # Original OpenAI config (commented out for testing)
  provider: openrouter
  model: o4-mini
  apiKey: $OPENROUTER_API_KEY
  maxIterations: 50

# System prompt
systemPrompt: "You are a helpful AI assistant with memory capabilities. Please confirm you're working with OpenRouter API."